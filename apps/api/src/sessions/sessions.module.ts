import { Module } from '@nestjs/common';

/**
 * Sesiones de clase: instancias concretas de una reserva en el calendario.
 *
 * TODO: controller, service y repositorio (pendiente de Prisma).
 */
@Module({})
export class SessionsModule {}
