import { Module } from '@nestjs/common';

/**
 * Operaciones de back-office reservadas al rol ADMIN.
 *
 * TODO: controller y guards de rol (dependen de AuthModule).
 */
@Module({})
export class AdminModule {}
